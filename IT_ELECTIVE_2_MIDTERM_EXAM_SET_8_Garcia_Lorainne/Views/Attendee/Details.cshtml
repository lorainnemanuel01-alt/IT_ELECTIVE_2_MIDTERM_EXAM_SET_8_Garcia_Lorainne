@model IT_ELECTIVE_2_MIDTERM_EXAM_SET_8_Garcia_Lorainne.Models.AttendeeVisit

@{
    ViewData["Title"] = "Attendee Details";
}

<div class="container mt-4">

    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="card shadow">

                <div class="card-header">
                    <h3 class="mb-0">Attendee Details</h3>
                </div>

                <div class="card-body">

                    <dl class="row">

                        <dt class="col-sm-4">
                            Ticket Number
                        </dt>
                        <dd class="col-sm-8">
                            @Model.TicketNumber
                        </dd>

                        <dt class="col-sm-4">
                            First Name
                        </dt>
                        <dd class="col-sm-8">
                            @Model.FirstName
                        </dd>

                        <dt class="col-sm-4">
                            Last Name
                        </dt>
                        <dd class="col-sm-8">
                            @Model.LastName
                        </dd>

                        <dt class="col-sm-4">
                            Company/School
                        </dt>
                        <dd class="col-sm-8">
                            @Model.Organization
                        </dd>

                        <dt class="col-sm-4">
                            Contact Number
                        </dt>
                        <dd class="col-sm-8">
                            @Model.ContactNumber
                        </dd>

                        <dt class="col-sm-4">
                            Email
                        </dt>
                        <dd class="col-sm-8">
                            @Model.Email
                        </dd>

                        <dt class="col-sm-4">
                            Event Name
                        </dt>
                        <dd class="col-sm-8">
                            @Model.EventName
                        </dd>

                        <dt class="col-sm-4">
                            Check-In Time
                        </dt>
                        <dd class="col-sm-8">
                            @Model.CheckInTime.ToString("MMM dd, yyyy hh:mm tt")
                        </dd>

                        <dt class="col-sm-4">
                            Check-Out Time
                        </dt>
                        <dd class="col-sm-8">
                            @if (Model.CheckOutTime.HasValue)
                            {
                                @Model.CheckOutTime.Value.ToString("MMM dd, yyyy hh:mm tt")
                            }
                            else
                            {
                                <span class="text-muted">
                                    Not checked out
                                </span>
                            }
                        </dd>

                        <dt class="col-sm-4">
                            Status
                        </dt>
                        <dd class="col-sm-8">
                            @if (Model.Status == "Present")
                            {
                                <span class="badge bg-success">
                                    Present
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">
                                    Left Event
                                </span>
                            }
                        </dd>

                        <dt class="col-sm-4">
                            Notes
                        </dt>
                        <dd class="col-sm-8">
                            @if (string.IsNullOrWhiteSpace(Model.Notes))
                            {
                                <span class="text-muted">
                                    No notes
                                </span>
                            }
                            else
                            {
                                @Model.Notes
                            }
                        </dd>

                    </dl>

                    <hr />

                    <div class="d-flex gap-2">

                        <a asp-action="Edit"
                           asp-route-id="@Model.Id"
                           class="btn btn-warning">
                            Edit
                        </a>

                        @if (Model.Status == "Present")
                        {
                            <a asp-action="CheckOut"
                               asp-route-id="@Model.Id"
                               class="btn btn-danger">
                                Check-Out
                            </a>
                        }

                        <a asp-action="Index"
                           class="btn btn-secondary">
                            Back to List
                        </a>

                    </div>

                </div>

            </div>

        </div>
    </div>

</div>